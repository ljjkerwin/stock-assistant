name: 语法检查

on:
  push:
    branches:
      - develop
    paths:
      - 'frontend/src/**'
      - 'frontend/package.json'
      - 'frontend/tsconfig*.json'
      - 'frontend/.eslintrc*'
      - 'backend/src/**'
      - 'backend/package.json'
      - 'backend/tsconfig*.json'
      - 'backend/.eslintrc*'
  pull_request:
    branches:
      - main
    paths:
      - 'frontend/src/**'
      - 'frontend/package.json'
      - 'frontend/tsconfig*.json'
      - 'frontend/.eslintrc*'
      - 'backend/src/**'
      - 'backend/package.json'
      - 'backend/tsconfig*.json'
      - 'backend/.eslintrc*'
  workflow_dispatch:
    inputs:
      scope:
        description: '检查范围'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - frontend
          - backend

jobs:
  frontend-lint:
    name: 前端语法检查
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'push' ||
      github.event_name == 'pull_request' ||
      inputs.scope == 'all' || inputs.scope == 'frontend'
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: 设置 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: 安装依赖
        run: pnpm install --frozen-lockfile

      - name: ESLint 检查
        run: pnpm lint

      - name: TypeScript 类型检查
        run: pnpm exec tsc --noEmit

  backend-lint:
    name: 后端语法检查
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'push' ||
      github.event_name == 'pull_request' ||
      inputs.scope == 'all' || inputs.scope == 'backend'
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - name: 设置 pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: backend/pnpm-lock.yaml

      - name: 安装依赖
        run: pnpm install --frozen-lockfile

      - name: ESLint 检查
        run: pnpm lint

      - name: TypeScript 类型检查
        run: pnpm exec tsc --noEmit
